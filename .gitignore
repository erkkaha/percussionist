# Build artifacts
*.tar
*.tar.gz

# VitePress build cache
docs/.vitepress/cache/

# Local env / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
.opencode/

# Node (for upcoming M2+ packages)
node_modules/
dist/
.turbo/
*.tsbuildinfo

# Bundled CLI binaries (produced by `pnpm --filter @percussionist/cli bundle`)
packages/cli/bin/

# Kubernetes
kubeconfig
*.kubeconfig

# Machine-specific kustomize overlay (hostnames, image tags, local tweaks).
# Start from k8s/local.example/.
/k8s/local/

# Runtime data (SQLite stats DB, etc.)
packages/web/data/

prds/
*.bak

# Agent-generated plan artifacts (risk surface for secrets)
.percussionist/
