# Default tmux config for percussionist runner pods.
# Users can override via ~/.tmux.conf in the agent home directory.

set -g default-terminal xterm-256color
set -g mouse on
set -g history-limit 50000
set -g allow-rename off
