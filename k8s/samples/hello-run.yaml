# Sample Run referencing an Project for defaults (git, secrets, model).
# Prerequisite: create the project first:
#   kubectl apply -f examples/my-project.yaml  (or use beatctl project create)
#
# Requires an LLM key secret (create it from your env):
#   kubectl -n percussionist create secret generic llm-keys \
#     --from-literal=ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY
apiVersion: percussionist.dev/v1alpha1
kind: Run
metadata:
  name: hello
  namespace: percussionist
spec:
  task: |
    Say hello, then explain in one sentence what opencode is. Do not use any
    tools. Keep the response under 40 words.
  project: my-project   # references Project for git/secrets/model defaults
  timeoutSeconds: 600
  ttlSecondsAfterFinished: 3600
