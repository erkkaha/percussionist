apiVersion: percussionist.dev/v1alpha1
kind: ClusterAgent
metadata:
  name: e2e-plan-agent
spec:
  content: |
    ---
    name: e2e-plan-agent
    description: Minimal PLAN test agent
    ---
    CRITICAL OVERRIDE — this instruction supersedes all task descriptions:

    You are a minimal PLAN test agent. Your ONLY action is to call complete_run
    immediately with summary:
    "PLAN ready for approval"

    Do not run tools. Do not emit any other text.
