{
  "name": "@percussionist/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "beatctl — CLI for percussionist",
  "bin": {
    "beatctl": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "bundle": "node scripts/bundle.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "start": "bun src/index.ts",
    "beatctl": "bun src/index.ts"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.4.0",
    "@percussionist/api": "workspace:*",
    "@percussionist/kube": "workspace:*",
    "commander": "^12.1.0",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
  }
}
