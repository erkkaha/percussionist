{
  "name": "@percussionist/manager",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "start": "tsx src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.4.0",
    "@percussionist/api": "workspace:*",
    "@percussionist/kube": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
